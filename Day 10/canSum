#include <bits/stdc++.h>
#include <vector>
using namespace std;

bool canSum(int n, int k, vector<long long int>& ipt) {
    
    vector<bool> dp(k+1, false);
    
    dp[0] = true;
    
    for(int i = 0;i <= k;i++){
        if (dp[i]){
            for(int j = 0;j < n;j++){
                if( i+ipt[j] <= k ) {
                    dp[ i+ipt[j] ] = true;
                }
            }
        }
    }
    
    return dp[k];
}

int main() {
    int k;
    cin >> k;
    vector<long long int> ipt;
    
    string input;
    cin >> input;
    input = input.substr(1, input.size()-2);
    stringstream ss(input);
    string data;
    while(getline(ss, data, ',')){
        ipt.push_back(stoi(data));
    }
    
    if(canSum(ipt.size(), k, ipt)) cout << "true";
    else cout << "false";
    
    return 0;
}
